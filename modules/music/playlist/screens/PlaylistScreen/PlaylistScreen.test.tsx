describe('Playlist screen', () => {
  it('should render the component as expected');
  it('should receive from the navigation a playlist id');
  it('should request a backend call to retrieve the songs of that playlist');
});
