import React, { ReactNode } from 'react';
// import MockAdapter from 'axios-mock-adapter';

describe('music API', () => {
  describe('playlist', () => {
    test('useGetFeaturedPlaylists', () => {});
    test('useGetPlaylistDetail', () => {});
  });
});
